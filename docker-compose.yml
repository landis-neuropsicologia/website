version: '3.8'

services:
  website:
    build: .
    ports:
      - "8080:80"
    container_name: landis-website
    restart: unless-stopped
    volumes:
      # Opcional: bind mount para desenvolvimento
      - ./css:/usr/share/nginx/html/css:ro
      - ./assets:/usr/share/nginx/html/assets:ro
